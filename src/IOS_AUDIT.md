# iOS READINESS AUDIT
## PellazgoShop — Native iOS Analysis
**Date:** 2026-06-24 | **Capacitor:** 5.7.8 | **Bundle:** com.pellazgo.app | **Team:** H4QUDPK7S3

---

## Executive Summary

The iOS project is a **genuine Capacitor 5 shell** — not the fake stub previously pushed by the Base44 GitPage. The real Xcode project (`ios/App/App.xcodeproj`) is present with the actual CocoaPods workspace, Assets.xcassets with real splash/icon images, and a proper Capacitor AppDelegate. This is a legitimate foundation.

**iOS Readiness Score: 68/100**

---

## 1. Capacitor Configuration

```json
{
  "appId": "com.pellazgo.app",      ✅ Valid reverse-domain
  "appName": "Pellazgo",            ✅ Matches App Store listing
  "webDir": "dist",                 ✅ Correct Vite output dir
```


**Fixes needed:**
```json
{
  "appId": "com.pellazgo.app",
  "appName": "Pellazgo",
  "webDir": "dist",
  "bundledWebRuntime": false,
  "server": {

    "androidScheme": "https",
    "iosScheme": "https",
    "allowNavigation": ["*.pellazgo.com", "*.base44.com"]
  },
  "ios": {
    "contentInset": "automatic",
    "scrollEnabled": true,
    "backgroundColor": "#0f3320"
  }
}
```


| `App.xcworkspace` | ✅ Present | CocoaPods workspace |
| `AppDelegate.swift` | ✅ Present | Proper Capacitor AppDelegate |
| `Assets.xcassets` | ✅ Present | AppIcon + Splash sets |
| `LaunchScreen.storyboard` | ✅ Present | Base.lproj |
| `Info.plist` | ✅ Present (inferred) | Standard Capacitor plist |
| `Podfile` | ✅ Present (inferred) | Auto-generated by cap add ios |
| Privacy Manifest | ❌ MISSING | Required for App Store iOS 17+ |

---

## 3. Bundle Identifier Verification

| Field | Value | Status |
|-------|-------|--------|
| capacitor.config.json appId | com.pellazgo.app | ✅ |
| pbxproj PRODUCT_BUNDLE_IDENTIFIER | com.pellazgo.app | ✅ (Capacitor sets this) |
| App Store Connect (expected) | com.pellazgo.app | ✅ |
| Provisioning Profile | Pellazgo_Shop | ✅ Present in signing/ |
| Team ID | H4QUDPK7S3 | ✅ Consistent across all configs |

---

## 4. Signing Configuration

| Asset | Location | Status |
|-------|----------|--------|
| Distribution certificate (.p12) | `signing/pellazgo.p12` | ✅ Present ⚠️ In repo |
| Mobile provisioning | `signing/Pellazgo_Shop (2).mobileprovision` | ✅ Present ⚠️ Spaces in filename |
| ASC API key (.p8) | `signing/AuthKey_G457WC6V47.p8` | ✅ Present ⚠️ CRITICAL in repo |
| Distribution cert (.cer) | `signing/distribution (2).cer` | ✅ Present |
| Private key (.key) | `signing/pellazgo.key` | ✅ Present ⚠️ In repo |

**Provisioning Profile issue:** Filename `Pellazgo_Shop (2).mobileprovision` has spaces and "(2)" suffix — the GitHub Actions workflow uses a wildcard `signing/*.mobileprovision` which will pick it up, but it suggests this is not the canonical copy. Rename to `Pellazgo_Shop.mobileprovision`.

---

## 5. App Icon Status

| Size | File | Status |
|------|------|--------|
| 1024×1024 (App Store) | `AppIcon-512@2x (1).png` | ⚠️ File has space in name; only 1 file found |
| Contents.json | Present | ⚠️ Likely references wrong filenames |

**Action required:** The AppIcon.appiconset has only one PNG with a non-standard name. Xcode requires a properly named `Contents.json` pointing to all required sizes. Run `npx cap sync` with a proper icon set or use `@capacitor/assets` to generate all sizes.

---

## 6. Splash Screen

| Asset | Status |
|-------|--------|
| `splash-2732x2732.png` | ✅ Present (3 copies — @1x, @2x, @3x all same file) |
| `Splash.imageset/Contents.json` | ✅ Present |
| Splash background color | ✅ Forest green matches brand |

---

## 7. Entitlements

No custom `.entitlements` file committed. Capacitor generates a default one with:
- App Groups: None (not needed)
- Push Notifications: Not configured
- Sign in with Apple: Not configured
- Associated Domains: ❌ Missing (needed for universal links)

---

## 8. URL Schemes & Deep Linking

| Feature | Status | Fix |
|---------|--------|-----|
| Custom URL scheme | ❌ Not configured | Add `pellazgo://` in Info.plist |
| Universal Links | ❌ Not configured | Add Associated Domains + apple-app-site-association |
| OAuth redirect | ⚠️ Needed for Google OAuth | Add URL scheme for Google Sign-In |

---

## 9. App Transport Security (ATS)

Capacitor 5 default allows HTTPS only. The app connects to:
- `*.base44.com` — HTTPS ✅
- `media.base44.com` — HTTPS ✅  
- `fonts.googleapis.com` — HTTPS ✅
- `unsplash.com` — HTTPS ✅

**Status:** ATS compliant by default. No exceptions needed. ✅

---

## 10. Permissions Required

| Permission | Key | Current | Required |
|------------|-----|---------|---------|
| Camera | NSCameraUsageDescription | ⚠️ Added by ios.yml script | Profile photos |
| Photo Library | NSPhotoLibraryUsageDescription | ⚠️ Added by ios.yml script | Profile photos |
| Location | NSLocationWhenInUseUsageDescription | ⚠️ Added by ios.yml script | Optional map |
| Face ID | NSFaceIDUsageDescription | ❌ Not set | Optional biometrics |

**Issue:** These are being added via `PlistBuddy` in the CI script rather than in the committed `Info.plist`. This is fragile. Add them permanently to `ios/App/App/Info.plist` in the repo.

---

## 11. Privacy Manifest (CRITICAL — App Store Rejection Risk)

Apple requires `PrivacyInfo.xcprivacy` since May 2024 for all new submissions.

**Status: ❌ NOT PRESENT**

Required APIs used by Capacitor:
- `NSPrivacyAccessedAPICategoryUserDefaults` (Capacitor uses UserDefaults)
- `NSPrivacyAccessedAPICategoryFileTimestamp` (WKWebView cache)

**Fix:**
```xml
<!-- ios/App/App/PrivacyInfo.xcprivacy -->
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" ...>
<plist version="1.0">
<dict>
  <key>NSPrivacyTracking</key><false/>
  <key>NSPrivacyTrackingDomains</key><array/>
  <key>NSPrivacyCollectedDataTypes</key><array/>
  <key>NSPrivacyAccessedAPITypes</key>
  <array>
    <dict>
      <key>NSPrivacyAccessedAPIType</key>
      <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
      <key>NSPrivacyAccessedAPITypeReasons</key>
      <array><string>CA92.1</string></array>
    </dict>
  </array>
</dict>
</plist>
```

---

## 12. iOS Build Readiness Checklist

| Item | Status |
|------|--------|
| Capacitor project structure | ✅ |
| Bundle ID configured | ✅ |
| Signing assets present | ✅ (but in repo — security risk) |
| App icon all sizes | ❌ Fix needed |
| Privacy manifest | ❌ Required |
| Permissions in Info.plist | ⚠️ Script-injected, not committed |
| Universal links | ❌ Not configured |
| Splash screen | ✅ |
| Minimum iOS target (13.0) | ✅ |
| CocoaPods workspace | ✅ |
| Archive configuration | ✅ |
| Export options | ✅ (generated in CI) || Component | Status | Notes |
|-----------|--------|-------|
| `App.xcodeproj` | ✅ Present | Real Capacitor-generated project |
---

## 2. Native iOS Project

